new ModelTypeDefinition(
	"{{ type.Name }}",  
	[
		{% for p in type.Properties %}
			{% include "model-property", property: p %}{% unless forloop.last %}, {% endunless -%}
		{% endfor %}
	],
	{{ type.IsCollection }}, 
	{{ type.IsNullable }},
	[
		//{-% for st in type.SupportTypes %-}
		//	{-% include "model-type", type: st %-}{-% unless forloop.last %-}, {-% endunless -%}
		//{-% endfor %-}
	])