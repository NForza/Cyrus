using System;
using System.Threading;
using System.Threading.Tasks;

{% if Namespace != "" %}
namespace {{ Namespace }};
{%- endif %}

public partial class {{ ClassName }}
{
    public partial {{ ResultType }} {{ MethodName }}(
        {%- for arg in Arguments %}{{ arg.Type }} {{ arg.Name }}{% if forloop.last == false %}, {% endif %}{% endfor -%}
    ) 
    {
        {%- for step in Steps %}
            {% if step.IsVoid == false %} var step{{ step.Index }}Result = {%- endif %} {{ step.Name -}}
            (
                {%- for param in step.Parameters -%}
                        {{ param.Name }}{% if forloop.last == false %}, {% endif %}
                {%- endfor -%}
            );
        {%- endfor %}
    }
}
