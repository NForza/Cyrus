using System;
using MassTransit;
using NForza.Cyrus.Cqrs;
using NForza.Cyrus.MassTransit;
using Microsoft.Extensions.DependencyInjection;

namespace NForza.Cyrus.MassTransit;

{% for c in Consumers %}
public class {{ c.Name }}Consumer(EventHandlerDictionary eventHandlerDictionary, IServiceScopeFactory serviceScopeFactory) : EventConsumer<{{ c.FullName }}>(eventHandlerDictionary, serviceScopeFactory)
{ }
{% endfor %}
