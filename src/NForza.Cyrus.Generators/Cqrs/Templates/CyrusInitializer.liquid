using Microsoft.Extensions.DependencyInjection;
using NForza.Cyrus;
{% for t in Usings -%}
using {{ t }};
{% endfor -%}

namespace NForza.Cyrus.{{ Namespace }};

#nullable enable

public class {{ Name }} : ICyrusInitializer
{
    public void RegisterServices(IServiceCollection services)
    {
        {{ Initializer }}
    }
}