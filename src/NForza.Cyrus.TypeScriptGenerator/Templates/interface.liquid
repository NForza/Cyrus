{% for t in Imports -%}
import { {{ t.Name }} } from './{{ t.Name }}';
{% endfor -%}

export interface {{ Name }} {
{%- for prop in Properties %}
    {{ prop.Name | camel-cased }}{% if prop.IsNullable %}?{% endif %}: {{ prop.Type | to-tstype }}{% if prop.IsCollection %}[]{% endif %};
{%- endfor %}
}
