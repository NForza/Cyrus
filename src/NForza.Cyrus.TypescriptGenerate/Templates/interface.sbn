{{ for t in Imports }}
import { {{ t.Name }} } from './{{ t.Name }}';
{{ end }}

export class {{ Name }} {
{{ for prop in Properties }}
    public {{ prop.Name }}{{ if prop.IsNullable }}?{{ end }}: {{ prop.Type | to_typescript_type }}{{ if prop.IsCollection }}[]{{ end }};
{{ end }}
}